{
  "name": "jackdpeterson/continuous-deployment-sample-project",
  "description": "A sample project that has CodeDeploy configuration in place and renders out PHP Info from the webserver",
  "type": "project",
  "license": "MIT",
  "authors": [
    {
      "name": "Jack D Peterson",
      "email": "jack.peterson@gmail.com"
    }
  ],
  "require": {
    "php": "^7.2"
  },
  "require-dev": {
    "phpunit/phpunit": "^7.0"
  },
  "scripts": {
    "test-phpdbg": "phpdbg -qrr ./vendor/bin/phpunit -c ./phpunit.xml --bootstrap ./vendor/autoload.php",
    "test": "./vendor/bin/phpunit -c ./phpunit.xml --bootstrap ./vendor/autoload.php"
  }
}
